{
  "name": "persona-builder",
  "version": "1.0.0",
  "description": "Build a Firefox Persona with browser history, and snapshots of HTML.",
  "main": "background.js",
  "scripts": {
    "start": "web-ext run --config ./.web-ext-config.js",
    "lint": "web-ext lint --config ./.web-ext-config.js",
    "build": "web-ext build --overwrite-dest --config ./.web-ext-config.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^9.2.0"
  }
}
